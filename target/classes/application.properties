spring.application.name=descripciones-minimas

# Configuración de la base de datos
spring.datasource.url=jdbc:mysql://localhost:3306/minimas?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Jdiego8747*.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=0
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.sql.init.mode=always

# Eureka client configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.fetchRegistry=true
eureka.client.registerWithEureka=true
eureka.instance.prefer-ip-address=true

# Datos de inicialización
spring.sql.init.schema-locations=classpath:/db/schema.sql
spring.sql.init.data-locations=classpath:/db/data.sql


# microservicio.rutas.url=http://localhost:9090/rutas

# management.endpoints.web.exposure.include=health,info
# management.endpoint.health.show-details=always